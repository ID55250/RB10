import Link from 'next/link';
import data from '@/data/players.json';
export const metadata={title:'Data & image credits · RB10 Club guesser'};
export default function Credits(){
 return <main className="game-shell"><header className="masthead"><Link className="brand" href="/"><span className="brand-mark">RB<span>10</span></span><span className="brand-title">CLUB GUESSER</span></Link><Link className="secondary" href="/">Back to game</Link></header><article className="credits"><div className="eyebrow">FIXED DATA SNAPSHOT · {data.checkedAt}</div><h1>Data & image credits</h1><p>The game uses a manually reviewed snapshot of Wikipedia player and club records. Eligibility was checked against the 2026–27 Premier League, La Liga, Bundesliga, Serie A and Ligue 1 team lists. Only senior first-team clubs count; loan clubs count. The active club and an active parent club are excluded.</p><p>Player photographs are served from Wikimedia and retain the license shown below. Images are cropped only by the game layout. Club crests are shown for identification; their respective clubs and rights holders retain all rights.</p><h2>Player sources and photographs</h2><ol>{data.players.map(player=><li key={player.id}><strong>{player.name}</strong> · {player.currentClub}<small><a href={player.source} target="_blank" rel="noreferrer">Player record</a>{player.photoCredit&&<> · Photo by <a href={player.photoCredit.url||player.source} target="_blank" rel="noreferrer">{player.photoCredit.artist}</a> · <a href={player.photoCredit.licenseUrl||player.photoCredit.url||player.source} target="_blank" rel="noreferrer">{player.photoCredit.license}</a></>}</small></li>)}</ol></article></main>;
}
